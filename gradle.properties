# JVM 参数
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# 使用 AndroidX
android.useAndroidX=true
# 非传递性 R 类，加快构建
android.nonTransitiveRClass=true
# 工程路径包含中文字符（工作文件夹名），跳过 AGP 的非 ASCII 路径检查
android.overridePathCheck=true
# Kotlin 代码风格
kotlin.code.style=official
